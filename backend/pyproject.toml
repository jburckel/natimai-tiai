[project]
name = "tiai-backend"
version = "0.1.0"
description = "Tiai — backend API + worker"
requires-python = ">=3.12,<4.0"
dependencies = [
    "fastapi[standard]>=0.114,<1.0",
    "uvicorn[standard]>=0.30,<1.0",
    "sqlmodel>=0.0.21,<1.0",
    "psycopg[binary]>=3.1,<4.0",
    "alembic>=1.13,<2.0",
    "pydantic>=2.7,<3.0",
    "pydantic-settings>=2.2,<3.0",
    "httpx>=0.27,<1.0",
    "bcrypt>=5.0,<6.0",
    "pyjwt>=2.8,<3.0",
    "openpyxl>=3.1,<4.0",
    "tzdata>=2024.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0,<10.0",
    "pytest-asyncio>=1.0,<2.0",
    "pytest-cov>=7.0,<8.0",
    "ruff>=0.5,<1.0",
    "mypy>=2.0,<3.0",
    "types-openpyxl>=3.1",
]

[tool.uv]
# Application, not a distributable package: uv manages the env, no build step.
package = false

[tool.ruff]
target-version = "py312"
exclude = ["app/alembic"]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "C4", "UP"]
ignore = [
    "E501",  # line length
    "B008",  # function calls in argument defaults (FastAPI Depends/Query)
]

[tool.mypy]
strict = true
exclude = ["app/alembic"]
plugins = ["pydantic.mypy"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"
# House rule: session.exec() everywhere, never session.execute() — SQLModel's
# exec() covers SELECT and DML alike (and types CursorResult.rowcount), so the
# old deprecation-warning filter for execute() is gone with its last caller.

[tool.coverage.run]
source = ["app"]
omit = ["app/alembic/*", "app/scripts/*"]
# SQLAlchemy's async layer runs handler code across greenlet switches; without
# this, coverage loses track of lines after each await and under-reports.
concurrency = ["thread", "greenlet"]

[tool.coverage.report]
show_missing = true
# CI fails below this line. Currently ~71%; raise as the suite grows.
fail_under = 65
